
//    - Inside `Contact.js`, create a form with fields for name, email, and message.
//    - Add input fields and a textarea for the message.

// Add form validation:
//    - Use React's built-in form validation or a library like `react-hook-form`.
//    - Display a validation message if the user leaves a field empty or enters an invalid email.

// Optional: Add your contact info:
//    - Display your email and phone number below the form as a secondary way to reach you.
function Contact() {
    return (
      <div>
        <h1>Contact Me</h1>
        <p>This is the contact page.</p>
      </div>
    );
  }
  
  export default Contact; 